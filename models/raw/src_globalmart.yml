version: 2

sources:
  - name: global_mart
    database: raw
    schema: global_mart
    tables:
      - name: customers
      - name: orders
        columns:
          - name: orderid
            tests:
              - unique
              - not_null
      - name: product
